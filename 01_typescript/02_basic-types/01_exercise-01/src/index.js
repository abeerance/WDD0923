"use strict";
const userName = "My username";
const age = 26;
const isActive = true;
const hobbies = ["Music", "Gaming", "Reading"];
const userInformation = [6, userName, isActive];
const greetUser = (userName) => {
    console.log(`Hello ${userName}, welcome to the webapp`);
};
greetUser(userName);
const numberOrNull = null;
